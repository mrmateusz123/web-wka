import React from 'react';
import PropTypes from "prop-types";

const Input = props =>{
    const {vA, vB, vC} = props;
    return(
        <div>
            
        </div>
    )
}
Input.propTypes = {
    vA: PropTypes.string,
    vB: PropTypes.string,
    vC: PropTypes.string
};
export default Input;